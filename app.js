
      // function which return some JSX
      function HelloWorld() {
        return <h3>Hello World from React</h3>; //JSX
      }

      ReactDOM.render(<HelloWorld />, document.querySelector("#app"));

      //Comes from react-dom allows to connect our app to the DOM