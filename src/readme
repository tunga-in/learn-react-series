
nodejs: npm 

// npm install yarn -g

nodejs: runtime engine that allows to run js (not in the browser).

npm : node package manager
allows to install packages(online ) from npmjs.org


for you to manage version of you packages, track, pass command using npm .. WE need package.json file